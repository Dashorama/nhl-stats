
<!-- nexus:start -->
## Nexus Knowledge System — MANDATORY

You have MCP tools from `nexus-data-plane`. **You MUST use them.** This is not optional.

### REQUIRED: First thing every session
1. Call `get_user_context` — learn who the user is and how they like to work
2. Call `get_project_context` — see recent decisions, errors, and patterns

### REQUIRED: During work — log as you go
Call these tools **without being asked**. If you made a decision, log it. If you hit an error, log it. If you learned something, log it.

- `store_event` with type `decision` — when you choose an approach or architecture
- `store_event` with type `error` — when something fails or breaks
- `store_event` with type `fix` — when you solve a problem
- `store_event` with type `pattern` — when you notice a codebase convention
- `store_event` with type `note` — anything else worth remembering
- `remember` with type `preference` — when the user corrects you or says how they want things done
- `remember` with type `personal` — when you learn something about the user
- `query_knowledge` — before guessing, search for prior context

**If you are unsure whether to log something, log it.** Too much context is better than too little.

### REQUIRED: End of session
- Call `log_session_summary` — what was done, what's left, key decisions

### Tool reference
| Tool | When |
|------|------|
| `get_user_context` | Start of every session |
| `get_project_context` | Start of every session |
| `store_event` | Every decision, error, fix, pattern, or noteworthy moment |
| `remember` | Every user preference, correction, or personal fact |
| `query_knowledge` | Before guessing — search first |
| `log_session_summary` | End of every session |
| `bump_relevance` | When an event proves especially important |

(auto-generated by Nexus — do not edit between these markers)
<!-- nexus:end -->
